<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monitoryourhealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <!-- android:parentActivityName=".MainActivity"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".MainActivity" /> -->

        <activity android:name=".LoginPage" />
        <!-- android:parentActivityName=".HomePage"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".HomePage" /> -->

        <activity android:name=".SignUpPage" />
        <!-- android:parentActivityName=".HomePage"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".HomePage" /> -->

        <activity android:name=".MainPage" />
        <!-- android:parentActivityName=".LoginPage"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".LoginPage" /> -->
        <!-- </activity> -->


        <!-- <activity -->
        <!-- android:name=".NewLogin" -->
        <!-- android:parentActivityName=".SignUpPage"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".SignUpPage" /> -->
        <!-- </activity> -->


        <!-- <activity android:name=".MainPage" /> -->
        <!-- <activity android:name=".NewLogin"></activity> -->

        <activity android:name=".CurrentValue" />
        <activity android:name=".PreviousValue" />
        <activity android:name=".NormalValue" />
        <activity android:name=".NextCurrentPage" />
        <activity android:name=".NextPreviousPage" />
        <activity android:name=".NextNormalPage" />
        <activity android:name=".Check" />
        <activity android:name=".MainPage2" />
        <activity android:name=".CurrentValue2" />
        <activity android:name=".NormalValue2" />
        <activity android:name=".NextCurrentPage2" />
        <activity android:name=".NextNormalPage2"></activity>
    </application>

</manifest>